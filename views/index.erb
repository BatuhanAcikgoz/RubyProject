<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Batuhan Açıkgöz | Ruby Örnekleri</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f4f4f4;
      margin: 0;
      padding: 20px;
    }
    .container {
      background-color: #fff;
      border-radius: 8px;
      padding: 20px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      max-width: 800px;
      margin: auto;
    }
    h1 {
      text-align: center;
      color: #333;
    }
    .select-container {
      margin-top: 20px;
    }
    .select-container select {
      font-size: 16px;
      padding: 10px;
      border-radius: 5px;
      border: 1px solid #ccc;
      width: 100%;
      margin-top: 5px;
    }
    .select-container button {
      padding: 10px 20px;
      font-size: 16px;
      background-color: #007bff;
      color: #fff;
      border: none;
      border-radius: 5px;
      cursor: pointer;
      margin-top: 20px;
    }
    .select-container button:hover {
      background-color: #0056b3;
    }
  </style>
</head>
<body>

<div class="container">
  <h1>Çalıştırılacak Örneği Seçiniz</h1>

  <div class="select-container">
    <form action="/ornek" method="post">
      <label for="class_name">Örnek seçiniz:</label>
      <select name="class_name" id="class_name">
        <% classes.each do |cls| %>
          <option value="<%= cls[:name] %>"><%= cls[:name] %> (<%= cls[:class_description] %>)</option>
        <% end %>
      </select>
      <button type="submit">Örneği Seç</button>
    </form>
  </div>
</div>

</body>
</html>
